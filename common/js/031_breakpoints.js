/**
 * TODO: Build out a viewport trigger framework so we can do something like this:
 * (Should correspond to the scss breakpoint mixin behaviors, need to brainstorm implementation)
 *
 * Estarossa.breakpoint.min(Estarossa.breakpoint.sm, function() {
 *     // Do something when size crosses breakpoint from below (or on load)
 * });
 */

/*
Estarossa.breakpoint = (function() {
    var list = {
        xs: 480,
        sm: 768,
        md: 992,
        lg: 1200
    };
    /!**
     *
     * @param {number}  breakpoint  Viewport width
     * @param {function} callback   Callback method
     * @param {boolean} once        Fire first time only
     *!/
    var min = function(breakpoint, callback, once) {

    };
    var max = function(breakpoint, callback, once) {

    };
    var range = function(breakpoint, callback, once) {

    };
    return $.extend({}, list, {
        min: min,
        max: max,
        range: range
    });
})();
*/
